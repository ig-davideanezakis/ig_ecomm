<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 240 48" fill="none">
  <!-- Red accent mark -->
  <rect x="0" y="10" width="6" height="28" rx="3" fill="#ff0c3c" />
  <!-- Brand text — uses currentColor to adapt to theme -->
  <text x="20" y="34" font-family="system-ui, -apple-system, sans-serif" font-weight="800" font-size="28" letter-spacing="1" fill="currentColor">
    INFOGRAF
  </text>
  <!-- Since 1992 tagline -->
  <text x="152" y="36" font-family="system-ui, -apple-system, sans-serif" font-weight="500" font-size="10" letter-spacing="0.5" fill="currentColor" opacity="0.5">
    SINCE 1992
  </text>
</svg>
